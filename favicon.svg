<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <!-- The pink is sampled from the Branding keychain on the home page,
       so the tab picks up a colour the site already uses rather than an
       arbitrary one. Full-bleed circle: at 16px a favicon is barely more
       than a dot, and any inset would eat most of it. -->
  <circle cx="32" cy="32" r="32" fill="#D85888"/>
</svg>
